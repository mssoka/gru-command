# #74 r4 Part A — uncommitted repair checkpoint

**State:** HEAD remains `543d25c7c9d693c7c3ae299fbb83647214242d82` on `gru/perkins-indirect-fix-audit`. This is an uncommitted patch, **not** an accepted native verdict, new PR head, green full suite, or authorization for Part B. R1/r2 accepted records and r3/r4 unaccepted archives were not modified. No review, runner, deployment, model/config, ledger, #73, push or commit action occurred.

## Scoped change and finding disposition

| Finding | Disposition and evidence in this patch |
| --- | --- |
| r4 security 0 (blocker) | Repaired the M+A reused-source bypass: a frozen A-path proof now checks bounded plausible changed M/D prior blobs for exact/strong whole-caller reuse; an unchanged old line or multiple plausible sources is rejected. The red fixture quotes the **real** `const selected = nativeRef;`, asserts Git M+A, preflight/terminal rejection and no consolidated output. Green: genuinely new A caller with reader-derived line; edited-copy novel line; ambiguous double source rejects. Unrelated binary and >8 MiB changed source do not poison a small new caller. Git rename status alone never establishes authored evidence. Partial copies are outside whole-file attribution; prior-source scan bounds (256 changed paths/8 MiB inspected) fail closed when applicable. |
| r4 blind 0 | One-sided A/D patch now comes from the exact selected canonical frozen blob, not a Git pathspec that may include a directory's invalid descendant. D-only file→directory reader-derived proof passes. |
| r4 blind 1, edge 1, acceptance 1 | Final bare CR is preserved when followed by Git's no-newline marker; only actual CRLF termination strips CR. The bare-CR negative and existing added/removed CRLF positives pass. |
| r4 edge 0 | Fatal UTF-8 decoder preserves leading BOM via `ignoreBOM:true`; historical BOM-cited proof passes. Invalid UTF-8/NUL remain rejected. |
| r4 acceptance 0, codebase 0 | Prior-delta checks the *fully serialized nested bridge frame* with a 64 KiB margin below 1 MiB, in addition to the inner limit. Backslash-heavy direct native tool, offline real Pi adapter and actual MCP server+bridge pass below the cap and consistently reject above it. |
| r4 architecture 0 | **DEFERRED, nonblocking design work:** no range/cursor/pagination added. Smaller serialized cap does not make every 900 KiB–8 MiB deleted caller readable. If actual independent review needs inaccessible caller content, STOP rather than fabricate evidence. |
| r4 tests 0 + carried r2 prior 14 | Native production-parser full and delta submissions now have eight explicit malformed-proof cases each: missing/extra location keys, null, array, invalid change, newline path/evidence, extra audit key. Assert specific schema violations in preflight and terminal and absence of consolidation. Suite-shape pin raised for actual registered cases, not bypassed. |
| r4 tests 1 | Prior-only NUL and invalid-UTF-8 deleted caller with forced-text attributes reject at lead reader, preflight and terminal; no consolidated result. |
| carried r2 prior 5 | Original accepted prior **base/target** IDs bind the historical citation. Exact frozen prior blob or hunk data (including multiline/prefixed citations) is verified independently from current caller changed-line proof; legacy target-blob citations without a base still work. Multiline hunk, missing-base rejection and prior blob regressions pass. |
| r4 prior audit index 1 / r2 finding 1 | Different-file coverage proof remains an eligible fixed-with-`fix_location` audit under the candidate validator, not a fabricated installed four-key proof or an accepted r4 disposition. Independent review must still decide. |

## Verification trail (nonzero exits retained)

- Red `npx vitest run test/perkins-builtin-review.test.ts -t 'unchanged caller line copied from a rewritten source' --maxWorkers=1 --reporter=dot` — exit 1: before repair the terminal *accepted* unchanged `nativeRef`. Green equivalent three-source-fixture selection (`-t 'unchanged caller line copied from a rewritten source|genuinely new caller with tool-derived|novel line in an edited copy'`) — 3 passed.
- Red `npx vitest run test/perkins-builtin-review.test.ts -t 'prefixed original citation' --maxWorkers=1 --reporter=dot` — exit 1 (hunk-prefixed quote unlocatable). Green exact command — 1 passed. `-t 'multiline accepted prior quote'` — 1 passed.
- `-t 'only the deleted regular caller|final bare CR|preserves a BOM|original accepted citation|missing prior base'` on the same bounded test file — 5 passed. `-t 'caps the serialized prior-delta envelope'` — 1 passed, including offline Pi/MCP calls.
- `-t 'two matching changed sources|unrelated binary changed source|prior-only caller'` — **nonzero** despite 4 passing tests: Vitest reported an unhandled worker RPC `onTaskUpdate` timeout. Separate narrow `-t 'NUL prior-only caller'` and `-t 'invalid UTF-8 prior-only caller'` — 1 passed each; `-t 'unrelated oversized changed source'` — 1 passed.
- Native malformed full/delta cases each passed in bounded 2–4-case slices: `-t 'native (full|delta) malformed fix_location: .missing path.'` 2 passed; full `(missing path|extra key|null)` 3, full `(array|invalid change|newline path)` 3, delta counterparts 3 + 3, and `(full|delta) ... (newline evidence|extra audit key)` 4. An initial incorrectly quoted `-t` selector skipped all 139 cases (exit 0), and was **not** counted as verification.
- `npx vitest run test/perkins-lead-schema-compat.test.ts --maxWorkers=1 --reporter=dot` — 5 passed, 1 intentional live test skipped. `npx vitest run test/claude-adapter.test.ts -t 'ReviewMcpBridge bounded protocol failures|ReviewMcpBridge fail-closed guards' --maxWorkers=1 --reporter=dot` — 7 passed. `test/suite-shape.test.ts` initially failed on the newly added parameterized `.each` cases; after registering explicit cases and correcting the pin, bounded rerun — 2 passed.
- **Full affected file non-green:** `npx vitest run test/perkins-builtin-review.test.ts --maxWorkers=1 --reporter=dot` was stopped by the command's 900-second resource/time bound, without a Vitest terminal result (log `/tmp/perkins-r4-builtin-full.log`, 93 progress marks). It is **not** a pass and was **not** retried. Follow-up options for Gru: authorize a separately bounded affected-case partition or obtain full Linux synthetic merge-result CI only after a later stable commit; neither substitutes for independent review. No worker timeout inflation.
- `npm run lint` first failed (`no-useless-escape` in the new hunk matcher); fixed; final lint passed. `npm run typecheck`, `npm run build`, standalone `node tools/verify-perkins-resource.mjs .`, `npm pack --dry-run` (including prepack backend/web rebuild), and `git diff --check` passed. Policy and MCP-server source were unchanged; no integrity repin. Build rev still records the unchanged HEAD because this patch is intentionally uncommitted.

**Gate:** Gru inspects this uncommitted checkpoint and unresolved full-suite limitation. No claim of native clearance, exact-new-head CI or deliverable merge result. Part B remains inactive and any later CI/recovery requires a separate operations-layer directive.
